name: Build App

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
      with:
        submodules: true
    
    - name: set up JDK 17
      uses: actions/setup-java@v3
      with:
        java-version: '17'
        distribution: 'temurin'
        cache: gradle

    - name: Grant execute permission for gradlew
      run: chmod +x gradlew
      
    - name: Build with Gradle
      run: ./gradlew assemblePlayProdRelease --parallel

    - name: Sign APKs
      uses: ilharp/sign-android-release@v1.0.4 # Or use @nightly
      id: sign_app
      with:
        releaseDir: app/build/outputs/apk/playProd/release/
        signingKey: ${{ secrets.KEYSTORE }}
        keyAlias: ${{ secrets.ALIAS }}
        keyStorePassword: ${{ secrets.PASS }}
        keyPassword: ${{ secrets.PASS }}
        buildToolsVersion: 33.0.0

    - name: Remove extra files
      run: |
        find app/build/outputs/apk/playProd/release/ -type f -regex 'Signal-Android-play-prod-.*-release-unsigned-[0-9].[0-9](?!-signed).*' -delete
        rm app/build/outputs/apk/playProd/release/key.jks
    
    - name: Save APKs 
      uses: actions/upload-artifact@v4
      with:
        # Name of the artifact to upload.
        # Optional. Default is 'artifact'
        name: 'le soy'
    
        # A file, directory or wildcard pattern that describes what to upload
        # Required.
        path: app/build/outputs/apk/playProd/release/
    
        # The desired behavior if no files are found using the provided path.
        # Available Options:
        #   warn: Output a warning but do not fail the action
        #   error: Fail the action with an error message
        #   ignore: Do not output any warnings or errors, the action does not fail
        # Optional. Default is 'warn'
        if-no-files-found: warn
    
        # Duration after which artifact will expire in days. 0 means using default retention.
        # Minimum 1 day.
        # Maximum 90 days unless changed from the repository settings page.
        # Optional. Defaults to repository settings.
        retention-days: 2
    
        # The level of compression for Zlib to be applied to the artifact archive.
        # The value can range from 0 to 9.
        # For large files that are not easily compressed, a value of 0 is recommended for significantly faster uploads.
        # Optional. Default is '6'
        compression-level: 6
    
        # If true, an artifact with a matching name will be deleted before a new one is uploaded.
        # If false, the action will fail if an artifact for the given name already exists.
        # Does not fail if the artifact does not exist.
        # Optional. Default is 'false'
        overwrite: false
